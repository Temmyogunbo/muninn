[project]
name = "backend"
version = "0.1.0"
description = "Program reporter"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "database",
    "api",
    "reporter",
]
